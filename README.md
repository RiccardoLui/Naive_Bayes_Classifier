This project implements a Naive Bayes Classifier using R, with the purpose of identifying fake news from reliable ones. 

A jupyter notebook for downloading the dataset is presented, while the Bayes_Classifier notebook performs the preprocessing of the data, defines the algorithm, trains it and then tests it on the dataset.
Finally, an analysis of the performance of the algorithm can be found.
